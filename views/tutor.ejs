<%- include('partials/header') %>

<style>
  :root {
    --primary: <%= theme || "#22d3ee" %>;
    --secondary: #6366f1;
    --bg-dark: #0f172a;
    --card-bg: rgba(30, 41, 59, 0.7);
    --border-light: rgba(255, 255, 255, 0.1);
  }

  body {
    font-family: "Kantumruy Pro", sans-serif;
    background-color: var(--bg-dark);
    color: #f1f5f9;
    margin: 0;
    line-height: 1.6;
  }

  /* ១. Background បែបស្ងប់ស្ងាត់ (Calm Learning Environment) */
  #neural-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -20;
    opacity: 0.15;
    pointer-events: none;
  }

  /* ២. Card Design ថ្មី៖ ស្រឡះភ្នែក និងមានសណ្ដាប់ធ្នាប់ */
  .magic-card {
    background: var(--card-bg);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 30px;
    border: 1px solid var(--border-light);
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  }

  /* ៣. Mode Buttons៖ ធំ ស្រួលចុច និងច្បាស់ៗ */
  .mode-btn {
    background: rgba(255, 255, 255, 0.05);
    color: #94a3b8;
    border: 1px solid var(--border-light);
    padding: 1rem 2rem;
    border-radius: 20px;
    font-weight: 700;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-size: 0.8rem;
  }

  .mode-btn.active {
    background: var(--primary);
    color: #0f172a;
    border-color: var(--primary);
    box-shadow: 0 0 20px rgba(34, 211, 238, 0.3);
  }

  .btn-generate {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    color: #0f172a;
    font-weight: 800;
    padding: 1.2rem 2.5rem;
    border-radius: 20px;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .btn-generate:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 25px -5px rgba(34, 211, 238, 0.4);
  }

  /* ៤. Input Field៖ ធំច្បាស់ ងាយស្រួលវាយ */
  .input-custom {
    background: rgba(15, 23, 42, 0.8) !important;
    border: 2px solid var(--border-light) !important;
    border-radius: 24px !important;
    color: white !important;
    padding: 1.5rem 2rem !important;
    font-size: 1.2rem;
    outline: none;
    transition: all 0.3s ease;
  }

  .input-custom:focus {
    border-color: var(--primary) !important;
    box-shadow: 0 0 0 4px rgba(34, 211, 238, 0.1);
  }

  /* ៥. Result Area Stylings */
  .result-title {
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--primary);
    margin-bottom: 1.5rem;
  }

  .point-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border-light);
    border-radius: 24px;
    padding: 2rem;
    transition: 0.3s;
  }

  .point-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: var(--primary);
  }

  .shimmer-text {
    background: linear-gradient(90deg, #fff, var(--primary), #fff);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: shine 8s linear infinite;
  }

  @keyframes shine {
    to {
      background-position: 200% center;
    }
  }
</style>

<canvas id="neural-bg"></canvas>

<main class="max-w-7xl mx-auto px-6 py-12 relative">
  <header
    class="text-center mb-16 animate-in fade-in slide-in-from-top-10 duration-700"
  >
    <div
      class="inline-flex items-center gap-3 px-5 py-2 rounded-full bg-cyan-500/10 border border-cyan-500/20 mb-8"
    >
      <span class="w-2 h-2 rounded-full bg-cyan-400 animate-pulse"></span>
      <span class="text-cyan-400 text-xs font-bold uppercase tracking-widest"
        >AI Tutor Intelligence</span
      >
    </div>

    <h1
      class="text-5xl md:text-8xl font-black shimmer-text mb-8 italic uppercase leading-tight"
    >
      គ្រូជំនួយ AI
    </h1>

    <div class="max-w-3xl mx-auto">
      <p
        class="text-xl md:text-2xl font-light text-slate-300 leading-relaxed mb-10"
      >
        "រៀនឱ្យឆ្លាត យល់ឱ្យច្បាស់! ឱ្យអូណ AI បកស្រាយរឿងស្មុគស្មាញ
        <br />ឱ្យក្លាយជារឿង
        <span class="text-cyan-400 font-bold">សាមញ្ញបំផុត</span> សម្រាប់បង!"
      </p>
    </div>

    <div class="flex flex-wrap justify-center gap-6">
      <div
        class="flex items-center gap-3 bg-white/5 px-6 py-3 rounded-2xl border border-white/10"
      >
        <span class="text-xl">⚡</span>
        <span class="text-xs font-bold text-slate-400 uppercase tracking-widest"
          >រហ័សទាន់ចិត្ត</span
        >
      </div>
      <div
        class="flex items-center gap-3 bg-white/5 px-6 py-3 rounded-2xl border border-white/10"
      >
        <span class="text-xl">🧸</span>
        <span class="text-xs font-bold text-slate-400 uppercase tracking-widest"
          >ងាយស្រួលយល់</span
        >
      </div>
      <div
        class="flex items-center gap-3 bg-white/5 px-6 py-3 rounded-2xl border border-white/10"
      >
        <span class="text-xl">🎯</span>
        <span class="text-xs font-bold text-slate-400 uppercase tracking-widest"
          >ចំចំណុចសំខាន់</span
        >
      </div>
    </div>
  </header>

  <section
    class="magic-card p-8 md:p-12 mb-16 animate-in zoom-in-95 duration-700"
  >
    <div
      class="flex flex-col lg:flex-row justify-between items-center gap-10 mb-12"
    >
      <div class="text-left">
        <h2 class="text-2xl font-bold text-white mb-2">
          តើបងចង់រៀនអំពីអ្វី?
        </h2>
        <p class="text-slate-500 text-sm">
          សូមជ្រើសរើសកម្រិតនៃការបកស្រាយខាងក្រោម
        </p>
      </div>
      <div class="flex gap-4 w-full md:w-auto">
        <button
          onclick="setMode('kid', this)"
          class="mode-btn flex-1 md:flex-none active"
        >
          👶 បែបក្មេងៗ (ងាយបំផុត)
        </button>
        <button
          onclick="setMode('simple', this)"
          class="mode-btn flex-1 md:flex-none"
        >
          💡 បែបសាមញ្ញ (ខ្លីខ្លឹម)
        </button>
      </div>
    </div>

    <div class="relative group">
      <input
        type="text"
        id="topicInput"
        class="input-custom w-full placeholder:text-slate-700"
        placeholder="បញ្ចូលប្រធានបទ... (ឧ. ហេតុអ្វីមេឃពណ៌ខៀវ?)"
      />
      <button
        onclick="askTutor()"
        class="lg:absolute right-3 top-3 bottom-3 mt-6 lg:mt-0 w-full lg:w-auto btn-generate"
      >
        បង្កើតចំណេះដឹង
      </button>
    </div>
  </section>

  <div id="loading" class="hidden flex flex-col items-center py-20">
    <div class="relative w-20 h-20 mb-8">
      <div class="absolute inset-0 border-4 border-white/5 rounded-full"></div>
      <div
        class="absolute inset-0 border-4 border-t-cyan-500 rounded-full animate-spin"
      ></div>
    </div>
    <p
      class="text-cyan-500 font-bold tracking-[0.3em] animate-pulse uppercase text-xs"
    >
      កំពុងទាញយកទិន្នន័យចំណេះដឹង...
    </p>
  </div>

  <div
    id="answerArea"
    class="hidden space-y-12 animate-in slide-in-from-bottom-10 duration-700"
  >
    <div class="magic-card p-10 md:p-16 border-l-[12px] border-cyan-400">
      <h3 id="resTitle" class="result-title uppercase italic"></h3>
      <div
        id="resDesc"
        class="text-xl md:text-3xl font-light text-slate-200 leading-relaxed italic"
      ></div>
    </div>

    <div
      id="pointsContainer"
      class="grid grid-cols-1 md:grid-cols-2 gap-8"
    ></div>

    <div
      class="grid grid-cols-1 lg:grid-cols-3 gap-10 pt-10 border-t border-white/10"
    >
      <div class="lg:col-span-2 space-y-8">
        <h4
          class="text-sm font-bold text-yellow-500 uppercase tracking-widest flex items-center gap-4"
        >
          <span class="h-px flex-1 bg-yellow-500/20"></span> ឧទាហរណ៍ជាក់ស្ដែង
        </h4>
        <ul id="resExamples" class="space-y-6"></ul>
      </div>
      <div
        class="magic-card p-10 bg-cyan-400/5 text-center flex items-center justify-center"
      >
        <div>
          <span class="text-6xl mb-6 block">💡</span>
          <p
            id="resFunFact"
            class="text-lg text-cyan-200 italic font-medium leading-relaxed"
          ></p>
        </div>
      </div>
    </div>
  </div>
</main>

<%- include('partials/footer') %>

<script>
  const socket = io();
  let currentMode = "kid";

  // ១. Background Animation (រង្វើល និងយឺត មិនឱ្យវិលមុខ)
  const canvas = document.getElementById("neural-bg");
  const ctx = canvas.getContext("2d");
  let dots = [];
  let mouse = { x: null, y: null };

  window.addEventListener("mousemove", (e) => {
    mouse.x = e.clientX;
    mouse.y = e.clientY;
  });

  function init() {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    dots = [];
    for (let i = 0; i < 50; i++) {
      dots.push({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        vx: (Math.random() - 0.5) * 0.25,
        vy: (Math.random() - 0.5) * 0.25,
        size: Math.random() * 1.5,
      });
    }
  }

  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.fillStyle = "<%= theme || '#22d3ee' %>";
    ctx.strokeStyle = "rgba(34, 211, 238, 0.05)";
    dots.forEach((d) => {
      d.x += d.vx;
      d.y += d.vy;
      if (d.x < 0 || d.x > canvas.width) d.vx *= -1;
      if (d.y < 0 || d.y > canvas.height) d.vy *= -1;
      let dist = Math.hypot(mouse.x - d.x, mouse.y - d.y);
      if (dist < 180) {
        ctx.beginPath();
        ctx.moveTo(d.x, d.y);
        ctx.lineTo(mouse.x, mouse.y);
        ctx.stroke();
      }
      ctx.beginPath();
      ctx.arc(d.x, d.y, d.size, 0, Math.PI * 2);
      ctx.fill();
    });
    requestAnimationFrame(draw);
  }
  init();
  draw();
  window.addEventListener("resize", init);

  // ២. Tutor Logic
  function setMode(mode, btn) {
    currentMode = mode;
    document
      .querySelectorAll(".mode-btn")
      .forEach((b) => b.classList.remove("active"));
    btn.classList.add("active");
  }

  async function askTutor() {
    const topic = document.getElementById("topicInput").value.trim();
    const firebaseUid = "<%= user ? user.firebaseUid : '' %>";

    if (!topic)
      return Swal.fire({
        text: "ប្រាប់អូនមកថាចង់រៀនអី? 😂",
        icon: "info",
        confirmButtonColor: "#22d3ee",
      });
    if (!firebaseUid)
      return Swal.fire({
        text: "សូមចូលគណនីសិនបង!",
        icon: "warning",
        confirmButtonColor: "#22d3ee",
      });

    document.getElementById("loading").classList.remove("hidden");
    document.getElementById("answerArea").classList.add("hidden");

    socket.emit("ask_tutor", { topic, mode: currentMode, firebaseUid });
  }

  socket.on("tutor_result", (data) => {
    document.getElementById("loading").classList.add("hidden");
    document.getElementById("answerArea").classList.remove("hidden");
    document.getElementById("resTitle").innerText = data.title;

    // Typewriter effect for explanation
    typeWriter("resDesc", data.explanation);

    // Render Key Points
    document.getElementById("pointsContainer").innerHTML = data.key_points
      .map(
        (p) => `
  <div class="point-card">
    <div class="text-cyan-400 font-black text-xl mb-4 underline decoration-cyan-500/20">${p.label}</div>
    <p class="text-lg text-slate-400 font-light leading-relaxed">${p.desc}</p>
  </div>
`,
      )
      .join("");

    // Render Examples
    document.getElementById("resExamples").innerHTML = data.examples
      .map(
        (ex) => `
      <li class="flex items-start gap-4 text-slate-200 text-lg">
        <span class="text-cyan-400 mt-1">●</span>
        <span class="font-light">${ex}</span>
      </li>
    `,
      )
      .join("");

    document.getElementById("resFunFact").innerText =
      "ដឹងអត់ថា? " + data.fun_fact;
    document
      .getElementById("answerArea")
      .scrollIntoView({ behavior: "smooth" });
  });

  function typeWriter(id, text) {
    let i = 0;
    const el = document.getElementById(id);
    el.innerHTML = "";
    function type() {
      if (i < text.length) {
        el.innerHTML += text.charAt(i);
        i++;
        setTimeout(type, 15);
      }
    }
    type();
  }
</script>
