<%- include('partials/header') %>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap');

    :root {
        --primary: #8b5cf6;
        --primary-glow: rgba(139, 92, 246, 0.3);
        --bg-dark: #0f172a;
        --card-bg: rgba(30, 41, 59, 0.7);
    }

    body {
        background-color: var(--bg-dark);
        background-image: 
            radial-gradient(circle at 0% 0%, rgba(139, 92, 246, 0.15) 0%, transparent 50%),
            radial-gradient(circle at 100% 100%, rgba(6, 182, 212, 0.1) 0%, transparent 50%);
        color: #f1f5f9;
        font-family: 'Plus Jakarta Sans', -apple-system, sans-serif;
        min-height: 100-vh;
    }

    .glass-card {
        background: var(--card-bg);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
    }

    .stat-card-glow:hover {
        border-color: var(--primary);
        box-shadow: 0 0 20px var(--primary-glow);
        transform: translateY(-5px);
    }

    .avatar-ring {
        position: relative;
        padding: 5px;
        background: linear-gradient(135deg, var(--primary), #06b6d4);
        border-radius: 2rem;
    }

    .text-gradient {
        background: linear-gradient(135deg, #fff 0%, #94a3b8 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
</style>

<main class="max-w-6xl mx-auto px-5 py-8 md:py-16">
    <section class="glass-card rounded-[2.5rem] p-8 md:p-12 mb-10 transition-all duration-500">
        <div class="flex flex-col md:flex-row items-center gap-8">
            <div class="avatar-ring shadow-2xl">
                <img src="<%= user.photoURL && user.photoURL !== '' ? user.photoURL : 'https://api.dicebear.com/7.x/adventurer/svg?seed=' + user.displayName %>" 
                     class="w-28 h-28 md:w-36 md:h-36 rounded-[1.7rem] object-cover bg-slate-800" 
                     alt="Profile">
            </div>
            
            <div class="text-center md:text-left">
                <span class="px-4 py-1.5 rounded-full bg-purple-500/10 border border-purple-500/20 text-purple-400 text-xs font-bold uppercase tracking-widest">
                    Neural Rank: Master Learner
                </span>
                <h2 class="text-4xl md:text-6xl font-extrabold text-white mt-4 tracking-tight">
                    សួស្តី, <span class="text-gradient"><%= user.displayName %></span>!
                </h2>
                <p class="text-slate-400 mt-2 font-light text-lg">រីករាយដែលបានជួបមេម្ដងទៀត! តោះបន្តការរៀនសូត្រ...</p>
            </div>
        </div>
    </section>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 mb-12">
        <div class="glass-card stat-card-glow p-8 rounded-[2rem] transition-all duration-300 group">
            <div class="w-14 h-14 bg-purple-500/10 rounded-2xl flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform">📚</div>
            <h3 class="text-4xl font-bold text-white mb-1"><%= totalWisdom %></h3>
            <p class="text-slate-500 text-sm font-medium uppercase tracking-wider">មេរៀនសរុប</p>
        </div>

        <div class="glass-card stat-card-glow p-8 rounded-[2rem] transition-all duration-300 group">
            <div class="w-14 h-14 bg-cyan-500/10 rounded-2xl flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform">🤖</div>
            <h3 class="text-4xl font-bold text-white mb-1"><%= tutorCount %></h3>
            <p class="text-slate-500 text-sm font-medium uppercase tracking-wider">ការពិភាក្សាជាមួយ AI</p>
        </div>

        <div class="glass-card stat-card-glow p-8 rounded-[2rem] transition-all duration-300 group sm:col-span-2 md:col-span-1">
            <div class="w-14 h-14 bg-emerald-500/10 rounded-2xl flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform">⚡</div>
            <h3 class="text-4xl font-bold text-emerald-400 mb-1">Active</h3>
            <p class="text-slate-500 text-sm font-medium uppercase tracking-wider">ស្ថានភាពបច្ចុប្បន្ន</p>
        </div>
    </div>

    <div class="space-y-6">
        <div class="flex items-center gap-4 mb-8">
            <h4 class="text-sm font-bold text-slate-400 uppercase tracking-[0.3em] whitespace-nowrap">សកម្មភាពចុងក្រោយ</h4>
            <div class="h-px w-full bg-gradient-to-r from-slate-800 to-transparent"></div>
        </div>

        <div class="grid gap-4">
            <% recentActivity.forEach(act => { %>
                <div class="glass-card p-5 md:p-6 rounded-2xl flex flex-col md:flex-row justify-between items-start md:items-center gap-4 hover:bg-white/5 transition-colors border-l-4 border-l-purple-500">
                    <div class="flex-1">
                        <p class="text-slate-200 font-medium text-lg leading-relaxed">"<%= act.userInput %>"</p>
                        <div class="flex items-center gap-3 mt-2">
                            <span class="text-[11px] bg-slate-800 px-2 py-0.5 rounded text-slate-400 border border-slate-700 font-mono uppercase"><%= act.toolName %></span>
                            <span class="text-[11px] text-slate-500 italic">Neural Sync Complete</span>
                        </div>
                    </div>
                    <a href="/history" class="w-full md:w-auto text-center px-6 py-2 rounded-xl bg-white/5 hover:bg-purple-500 hover:text-white border border-white/10 text-purple-400 text-sm font-bold transition-all">
                        មើលលម្អិត →
                    </a>
                </div>
            <% }) %>
        </div>
    </div>
</main>

<%- include('partials/footer') %>